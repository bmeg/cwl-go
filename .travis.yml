language: go
go:
  - tip
os:
  - linux
before_install:
  - sudo apt-get update
  - sudo apt-get install python-virtualenv
  - sudo pip install -U setuptools
install:
  - make depends
  - make 
script: make test
notifications:
  email: false